{{#if favItems}}
<ol class="group-list ">
	<li>
		<div class="group-list-title group-grid-fav-items">
			<h3 class="item-name">Items</h3>
			<div class="item-detail item-uses">Charges</div>
			<div class="item-detail item-quantity">Qty</div>
			<div class="item-detail item-comps"></div>
			<div class="item-detail item-controls"></div>
		</div>

		<ol class="item-list">
			{{#each favItems as |item id|}}
			{{> "modules/SW5eCharacterSheetUpgrade/templates/item.hbs" item=item}}
			{{/each}}
		</ol>
	</li>
	{{#if favFeats}}
	<li>
		<div class="group-list-title group-grid-fav-items">
			<h3 class="item-name">Features &amp; Feats</h3>
			<div class="item-detail item-uses">Uses</div>
			<div class="item-detail item-quantity"></div>
			<div class="item-detail item-comps"></div>
			<div class="item-detail item-controls"></div>
		</div>

		<ol class="item-list">
			{{#each favFeats as |feat lvl|}}
			{{> "modules/SW5eCharacterSheetUpgrade/templates/item.hbs" item=feat}}
			{{/each}}
		</ol>
	</li>
	{{/if}}
	{{#if favPowers}}
	{{#each favPowers as |section lvl|}}
	{{#if section.powers}}
	{{#if lvl}}
	<li>
		<div class="group-list-title group-grid-fav-items">
			<h3 class="item-name">
				{{#if section.isCantrip}}At-Will Powers{{else}}Level {{lvl}} Powers{{/if}}
			</h3>

			<div class="item-detail item-uses">
				{{#if section.isCantrip}}&nbsp;
				{{else}}
				{{section.value}}
				/
				{{section.max}}
				{{/if}}
			</div>
			<div class="item-detail item-quantity"></div>
			<div class="item-detail item-comps"></div>
			<div class="item-detail item-controls"></div>
		</div>
		<ol class="item-list">
			{{#each section.powers as |power id|}}
			{{> "modules/SW5eCharacterSheetUpgrade/templates/item.hbs" item=power}}
			{{/each}}
		</ol>
	</li>
	{{/if}}{{/if}}
	{{/each}}
	{{/if}}
</ol>
{{/if}}
{{!-- {{#if favFeats}}
		<ol class="fav-feat">
			<li class="item inventory-header flexrow">
			  <h3>Features & Feats</h3>
				<div class="item-detail uses">Uses</div>
				<div class="item-detail item-controls">Control</div>
			</li>
			{{#each favFeats as |feat lvl|}}
				{{> "modules/SW5eCharacterSheetUpgrade/templates/item.hbs" item=feat}}
			{{/each}}
		</ol>
	{{/if}}
	{{#if favPowers}}
		<ol class="fav-power">
			{{#each favPowers as |section lvl|}}
				{{#if section.powers}}
					{{#if lvl}}
						<li class="item inventory-header flexrow">                        
							<h3>
								{{#if section.isCantrip}}Cantrips{{else}}Powers ~ Level {{lvl}}{{/if}}
							</h3>
							<div class="item-detail uses">Components</div>
							{{#if section.isCantrip}}
								<div class="item-detail item-controls">Control</div>
							{{else}}
								<span class="power-slots">
									<input type="text" ${inputStyle} data-target="data.powers.power{{lvl}}.value" value="{{section.value}}" placeholder="0" data-dtype="Number" disabled/>
									/
									<input type="text" ${inputStyle} data-target="data.powers.power{{lvl}}.max" value="{{section.max}}" placeholder="0" data-dtype="Number" disabled/>
								</span>
							{{/if}}
						</li>
							{{#each section.powers as |power id|}}
								{{> "modules/SW5eCharacterSheetUpgrade/templates/item.hbs" item=power}}
							{{/each}}
					{{/if}}
				{{/if}}
			{{/each}}
		</ol>
	{{/if}} --}}